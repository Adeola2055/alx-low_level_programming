#include "main.h"
/**
 * add - add two integers and return their result
 * @a: first number in the parameter,
 * @b: second number in the parameter.
 *
 * Return: sum of the to integer.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
