#include <stdio.h>
/**
 * _islower - Check if a character is lowercase.
 * @c: Character to input.
 *
 * Return: 1 on success.
 * if false returns .
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
