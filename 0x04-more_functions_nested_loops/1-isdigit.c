#include "main.h"
/**
 * _isdigit - check for a digit between 0 - 9
 * @c: digit to be check
 *
 * Return: Always 0 (success) or 1 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
